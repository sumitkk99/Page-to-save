Ab screenshots se structure clear ho gaya hai. Tumhare TODO-based student-service ko complete karna hai.

## 1. Student.java (replace complete file)

```java
package com.enrollment.studentservice.model;

import jakarta.persistence.*;
import java.util.HashSet;
import java.util.Set;

@Entity
@Table(name = "students")
public class Student {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String name;

    @Column(unique = true)
    private String email;

    @ElementCollection
    @CollectionTable(
            name = "student_enrollments",
            joinColumns = @JoinColumn(name = "student_id")
    )
    @Column(name = "course_id")
    private Set<Long> enrolledCourseIds = new HashSet<>();

    public Student() {
    }

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getEmail() {
        return email;
    }

    public void setEmail(String email) {
        this.email = email;
    }

    public Set<Long> getEnrolledCourseIds() {
        return enrolledCourseIds;
    }

    public void setEnrolledCourseIds(Set<Long> enrolledCourseIds) {
        this.enrolledCourseIds = enrolledCourseIds;
    }
}
```

---

# 2. StudentAlreadyExistsException.java

```java
package com.enrollment.studentservice.exception;

public class StudentAlreadyExistsException extends RuntimeException {

    public StudentAlreadyExistsException(String message) {
        super(message);
    }
}
```

---

# 3. StudentNotFoundException.java

```java
package com.enrollment.studentservice.exception;

public class StudentNotFoundException extends RuntimeException {

    public StudentNotFoundException(String message) {
        super(message);
    }
}
```

---

# 4. StudentService.java (replace complete file)

```java
package com.enrollment.studentservice.service;

import com.enrollment.studentservice.client.CourseClient;
import com.enrollment.studentservice.exception.StudentAlreadyExistsException;
import com.enrollment.studentservice.exception.StudentNotFoundException;
import com.enrollment.studentservice.model.Student;
import com.enrollment.studentservice.repository.StudentRepository;

import jakarta.transaction.Transactional;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

@Service
public class StudentService {

    private static final Logger logger =
            LoggerFactory.getLogger(StudentService.class);

    private final StudentRepository repository;
    private final CourseClient courseClient;

    public StudentService(StudentRepository repository,
                          CourseClient courseClient) {
        this.repository = repository;
        this.courseClient = courseClient;
    }

    public Student save(Student student) {

        logger.info("Saving student {}", student.getEmail());

        if (repository.existsByEmail(student.getEmail())) {
            throw new StudentAlreadyExistsException(
                    "Email already exists");
        }

        return repository.save(student);
    }

    public Student findById(Long id) {

        return repository.findById(id)
                .orElseThrow(() ->
                        new StudentNotFoundException(
                                "Student not found : " + id));
    }

    @Transactional
    public Student enrollInCourse(Long studentId,
                                  Long courseId) {

        Student student = findById(studentId);

        if (student.getEnrolledCourseIds().contains(courseId)) {
            throw new StudentAlreadyExistsException(
                    "Student already enrolled in course");
        }

        student.getEnrolledCourseIds().add(courseId);

        return repository.save(student);
    }
}
```

---

# 5. StudentController.java (replace complete file)

```java
package com.enrollment.studentservice.controller;

import com.enrollment.studentservice.client.CourseClient;
import com.enrollment.studentservice.model.Student;
import com.enrollment.studentservice.service.StudentService;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/students")
public class StudentController {

    private final StudentService studentService;
    private final CourseClient courseClient;

    private static final Logger logger =
            LoggerFactory.getLogger(StudentController.class);

    public StudentController(StudentService studentService,
                             CourseClient courseClient) {
        this.studentService = studentService;
        this.courseClient = courseClient;
    }

    @PostMapping
    public ResponseEntity<?> createStudent(
            @RequestBody Student student) {

        try {

            Student saved =
                    studentService.save(student);

            return new ResponseEntity<>(
                    saved,
                    HttpStatus.CREATED);

        } catch (Exception e) {

            return ResponseEntity.internalServerError()
                    .body(e.getMessage());
        }
    }

    @PutMapping("/{id}/enroll/{courseId}")
    public ResponseEntity<Student> enrollStudent(
            @PathVariable Long id,
            @PathVariable Long courseId) {

        Student student =
                studentService.enrollInCourse(
                        id,
                        courseId);

        return ResponseEntity.ok(student);
    }

    @GetMapping("/{id}")
    public ResponseEntity<?> getStudentById(
            @PathVariable Long id) {

        Student student =
                studentService.findById(id);

        return ResponseEntity.ok(student);
    }

    @GetMapping("/courses")
    public ResponseEntity<?> testFeignCourses(
            @RequestHeader("Authorization")
            String token) {

        return ResponseEntity.ok(
                courseClient.getAllCourses(token).getBody());
    }
}
```

---

# 6. StudentServiceApplication.java

Check this annotation exists:

```java
@EnableFeignClients
@SpringBootApplication
public class StudentServiceApplication {
}
```

---

# 7. application.properties

Add:

```properties
spring.application.name=student-service

server.port=8082

spring.datasource.url=jdbc:mysql://localhost:3306/studentdb
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

jwt.secret=mySecretKeymySecretKeymySecretKey123456
```

---

# 8. Run order

Start in this order:

### Eureka

```
8761
```

---

### Gateway

```
8080
```

---

### Course Service

```
8081
```

Check:

```
http://localhost:8081/api/courses
```

---

### Student Service

```
8082
```

---

# Postman Testing

## Login

POST

```
http://localhost:8082/auth/login
```

Body

```json
{
  "username":"admin"
}
```

Response:

```json
{
  "token":"dummy-token-for-admin"
}
```

---

## Create Student

POST

```
http://localhost:8082/api/students
```

Headers

```
Authorization : Bearer dummy-token-for-admin
```

Body

```json
{
  "name":"Rahul",
  "email":"rahul@gmail.com"
}
```

---

## Get Student

GET

```
http://localhost:8082/api/students/1
```

---

## Enroll Student

PUT

```
http://localhost:8082/api/students/1/enroll/1
```

---

## Test Feign Client

GET

```
http://localhost:8082/api/students/courses
```

Headers

```
Authorization : Bearer dummy-token-for-admin
```

---

Ab ye code lagane ke baad agar red underline ya compile error aaye to **StudentController.java ka error screenshot + pom.xml screenshot** bhej dena. Phir remaining compile issues bhi fix karwa denge.
