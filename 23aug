Apologies for the confusion. Let's maintain your existing service class as it is and just add the new logic where needed. I'll break it down so you know exactly where to add the new code.

### Existing Service Class (`getCustomerInfoService`)

```java
package onevz.soe.accountlanding.service.impl;

import onevz.soe.accountlanding.model.graphql.CustomerRequestType;
import onevz.soe.accountlanding.model.response.SoeJsonResponse;
import onevz.soe.accountlanding.service.RetrieveCustomerInfoService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import reactor.core.publisher.Mono;

@Service
public class getCustomerInfoService {

    @Autowired
    private RetrieveCustomerInfoService retrieveCustomerInfoService;

    public Mono<SoeJsonResponse> getCustomerInfo_Lines(CustomerRequestType request) {
        return retrieveCustomerInfoService.retrieveCustomerInfo_lines(request)
            .flatMap(customerResponse -> {
                // Add your customer profile processing logic here
                
                // Original logic continues...
            });
    }
}
```

### Integration of New Logic

Add the following lines of code to your existing service class:

1. **Step 1**: **Add the `BPMHelper1` dependency**

   Just after the `RetrieveCustomerInfoService` dependency, add the `BPMHelper1` dependency:
   
   ```java
   @Autowired
   private BPMHelper1 bpmHelper1;
   ```

2. **Step 2**: **Invoke the BPM service and handle the response**

   Inside the `flatMap` block where you already have `customerResponse`, add the following logic to get the location violation status and populate the `fwaLocationViolationLines` list:

   ```java
   return bpmHelper1.getLocationViolationStatus(AccountLandingConstants.CHANNEL_ID, request.getAccountNumber())
       .flatMap(getCasesResponse -> {

           // Initialize the list of FWALocationViolationLines
           List<FWALocationViolationLines> fwaLocationViolationLines = new ArrayList<>();

           // Implement logic to populate FWALocationViolationLines based on getCasesResponse
           if (getCasesResponse.getTotalCases() > 1) {
               // More than one case - Add generic message along with MTN
               for (String mtn : request.getMtnList()) {
                   FWALocationViolationLines violationLine = new FWALocationViolationLines();
                   violationLine.setMtn(mtn);
                   violationLine.setCaseStatus("GenericLVSpeedCap");
                   fwaLocationViolationLines.add(violationLine);
               }
           } else if (getCasesResponse.getTotalCases() == 1) {
               // Single case - Compare MTN and add specific message
               String impactedMtn = getCasesResponse.getCaseByIndex(0).getImpactedMtn();
               for (String mtn : request.getMtnList()) {
                   FWALocationViolationLines violationLine = new FWALocationViolationLines();
                   violationLine.setMtn(mtn);
                   if (mtn.equals(impactedMtn)) {
                       violationLine.setCaseStatus(getCasesResponse.getCaseByIndex(0).getStatus());
                   } else {
                       violationLine.setCaseStatus("GenericLVSpeedCap");
                   }
                   fwaLocationViolationLines.add(violationLine);
               }
           }

           // Add the populated fwaLocationViolationLines to the SOE response
           SoeJsonResponse soeJsonResponse = new SoeJsonResponse();
           soeJsonResponse.setFwaLocationViolationLines(fwaLocationViolationLines);

           // Add your customer profile processing logic here
           soeJsonResponse.setCustomerProfile(customerResponse.getData().getCustomerProfile());

           return Mono.just(soeJsonResponse);
       });
   ```

3. **Final Step**: **Combine the two `flatMap` chains**

   Make sure to combine the two `flatMap` blocks so that they flow sequentially. Here's how your method should look:

   ```java
   public Mono<SoeJsonResponse> getCustomerInfo_Lines(CustomerRequestType request) {
       return retrieveCustomerInfoService.retrieveCustomerInfo_lines(request)
           .flatMap(customerResponse -> {
               return bpmHelper1.getLocationViolationStatus(AccountLandingConstants.CHANNEL_ID, request.getAccountNumber())
                   .flatMap(getCasesResponse -> {

                       List<FWALocationViolationLines> fwaLocationViolationLines = new ArrayList<>();

                       if (getCasesResponse.getTotalCases() > 1) {
                           for (String mtn : request.getMtnList()) {
                               FWALocationViolationLines violationLine = new FWALocationViolationLines();
                               violationLine.setMtn(mtn);
                               violationLine.setCaseStatus("GenericLVSpeedCap");
                               fwaLocationViolationLines.add(violationLine);
                           }
                       } else if (getCasesResponse.getTotalCases() == 1) {
                           String impactedMtn = getCasesResponse.getCaseByIndex(0).getImpactedMtn();
                           for (String mtn : request.getMtnList()) {
                               FWALocationViolationLines violationLine = new FWALocationViolationLines();
                               violationLine.setMtn(mtn);
                               if (mtn.equals(impactedMtn)) {
                                   violationLine.setCaseStatus(getCasesResponse.getCaseByIndex(0).getStatus());
                               } else {
                                   violationLine.setCaseStatus("GenericLVSpeedCap");
                               }
                               fwaLocationViolationLines.add(violationLine);
                           }
                       }

                       SoeJsonResponse soeJsonResponse = new SoeJsonResponse();
                       soeJsonResponse.setFwaLocationViolationLines(fwaLocationViolationLines);

                       soeJsonResponse.setCustomerProfile(customerResponse.getData().getCustomerProfile());

                       return Mono.just(soeJsonResponse);
                   });
           });
   }
   ```

### Summary of Changes

- **Autowired the `BPMHelper1`**: Added dependency injection for the helper class.
- **Invoke `getLocationViolationStatus`**: Inside the `flatMap`, invoke this method and process its response to populate `fwaLocationViolationLines`.
- **Build SOE Response**: After processing, the response is built, including both the `fwaLocationViolationLines` and `customerProfile`.

With these steps, you can integrate the new logic without altering the structure of your existing service class.
