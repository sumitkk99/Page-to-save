@Test
public void fetchFeatureFlagsWithNullPromoOrderFlag() {
    // Mock the feature flag to return null for promoOrderFFlag
    when(featureFlag.isFeatureEnabled(
        eq(FeatureFlagConstants.MARKETING_APP_NAME),
        eq(FeatureFlagConstants.SWIFT_CFG_PROFILE),
        eq(FeatureFlagConstants.PROMO_ORDER_FLAG),
        eq(Boolean.FALSE)
    )).thenReturn(null);
    
    // Call the method
    StepVerifier.create(featureFlagHelper.fetchFeatureFlags())
        .assertNext(data -> assertThat(data).isNotNull())
        .expectComplete()
        .verify();
    // You may want to add more specific assertions here to verify
    // that the map contains the expected default value
}
